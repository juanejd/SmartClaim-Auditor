# SmartClaim Auditor — Backend

## Run

```bash
uv run uvicorn app.main:app --reload
```

Swagger UI: http://localhost:8000/docs

## Test

```bash
uv run pytest -v
```

## Lint

```bash
uv run ruff check .
```
